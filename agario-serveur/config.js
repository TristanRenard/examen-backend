
const gridSize = 200
const initialPlayerSize = 1
const foodSize = 0.3
const foodCount = 600
const foodValue = 1
const playerSpeed = 5
const maxplayerSize = 15
const obstacleCount = 160
const obstacleSize = 3
const port = 3001

module.exports = {
  gridSize,
  initialPlayerSize,
  foodSize,
  foodCount,
  foodValue,
  playerSpeed,
  maxplayerSize,
  obstacleCount,
  obstacleSize,
  port
}