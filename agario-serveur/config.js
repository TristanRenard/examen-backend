
const gridSize = 10
const initialPlayerSize = 1
const foodSize = 0.3
const foodCount = 5
const foodValue = 1
const playerSpeed = 5
const maxplayerSize = gridSize / 2
const obstacleCount = 2
const obstacleSize = 2
const port = 3001

module.exports = {
  gridSize,
  initialPlayerSize,
  foodSize,
  foodCount,
  foodValue,
  playerSpeed,
  maxplayerSize,
  obstacleCount,
  obstacleSize,
  port
}